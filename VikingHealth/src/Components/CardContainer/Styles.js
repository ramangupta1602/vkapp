import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  cardsContainerStyle: {
    width: "100%",
    flexDirection: "row",
    minHeight: 180,
  },

  columnStyle: {
    flex: 1,
  },
});

export default styles;
