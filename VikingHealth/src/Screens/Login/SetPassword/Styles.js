import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  textField: {
    height: 40,
    marginTop: 20,
    color: 'black',
    paddingTop: 20
  },
  container: {
    flex: 1,
    padding: 24,
    paddingTop: 10
  }
});
